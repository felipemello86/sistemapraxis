// Obrigatório pra um parallel route slot (@modal): quando nenhuma rota
// interceptada bate (ex: usuário está em /admin/crm/etapas), o slot não
// deve renderizar nada.
export default function Default() {
  return null;
}

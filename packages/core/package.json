{
  "name": "@praxis/core",
  "private": true,
  "version": "0.1.0",
  "description": "Identidade compartilhada da suíte Praxis: schema suite_core (Prisma), sessão única (cookie assinado) e resolução de tenant. Importado por todos os apps do monorepo — nunca copiado.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "typecheck": "tsc --noEmit",
    "tenant:create": "tsx scripts/create-tenant.ts",
    "seed:test": "tsx scripts/seed-test-tenant.ts",
    "import:bnbflex-users": "tsx scripts/import-bnbflex-users.ts",
    "import:housekeeping-data": "tsx scripts/import-housekeeping-data.ts",
    "seed:housekeeping-test": "tsx scripts/seed-housekeeping-test.ts",
    "grant:housekeeping-access": "tsx scripts/grant-housekeeping-access.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "jose": "^5.9.6",
    "bcryptjs": "^2.4.3",
    "date-fns": "^3.6.0",
    "pg": "^8.13.0",
    "firebase-admin": "^13.10.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.10"
  }
}
